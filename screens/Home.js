import React, { useState } from "react";
import { FlatList, SafeArea, SafeAreaView, View, Text } from "react-native";

import { COLORS, NFTData } from "../constants";

const Home = () => {
  return <SafeAreaView style={{ flex: 1 }}></SafeAreaView>;
};

export default Home;
